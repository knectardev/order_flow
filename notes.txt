Act as a Senior Market Microstructure Engineer. We are adding a fourth canonical watch: the "Value Edge Rejection" (🎯). This watch targets high-frequency mean reversion setups when price overextends past the Value Area but lacks the momentum to break out.

### 1. Goal
Implement the 'Value Edge Rejection' watch logic. This captures the "Rubber Band" snap-back effect from the VAH/VAL toward the POC.

### 2. Configuration (Add to constants.js)
Define `VALUE_REJECT_CELL`:
- **volState:** 2 or 3 (Active or Steady). We want "normal" participation, not a climactic crash.
- **depthState:** 2 or 3 (Normal or Deep). We want a healthy book that can facilitate a return to mean.
- **Tuning Defaults:**
    - `valueRejectLevelTicks`: 4 (Must be very close to the edge).
    - `valueRejectDeltaDivergence`: true (Requires price to be at a high/low while Delta is neutral or reversing).

### 3. Logic Implementation (canonical.js)
Create `evaluateValueEdgeReject()`. It must pass 5 gates:
1. **Regime Gate:** `volState` is 2-3 AND `depthState` is 2-3.
2. **Proximity Gate:** Last settled bar's Close is within 4 ticks of VAH (Value Area High) or VAL (Value Area Low).
3. **Rejection Gate:** - If at VAH: The bar must have a "wick" on top (High - Close > Close - Open).
    - If at VAL: The bar must have a "wick" on bottom (Close - Low > Open - Close).
4. **Volume Gate:** Volume is within 0.8x to 1.2x of the 10-bar average (we want "standard" volume, not a spike).
5. **Alignment:** Use `buildAlignment(..., 'fade')` logic. We are fading the edge, so we want the HTF context to suggest the market is in a "ranging" or "balanced" state.

### 4. UI & Visuals
- **Glyph:** Use the Target emoji (🎯) or an Arrow Curve (↩️).
- **Placement:** Place at the VAH/VAL boundary where the rejection occurred.
- **Matrix Mapping:** Highlight the (2,2) and (3,3) "Middle" cells as the watched territory for this strategy.
- **Banner Color:** Use a "Forest Green" or "Success Teal" to indicate a high-probability structural setup.

### 5. Integration
- Update `checkWatches()` to include `VALUE_EDGE_REJECT`.
- Ensure the 'Details' modal explains that this is a "Balance-based Mean Reversion" targeting a return to the Point of Control (POC).
- Ensure the `direction` returned is always toward the POC (Down if at VAH, Up if at VAL).