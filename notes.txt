This is the ultimate trading-dev dilemma: you've found "The Ghost in the Machine"—a specific logic variation that is printing money—but you can't quite see its face.

If your **Bullseyes (349 fires)** are significantly fewer than the **Backtest Fallback (1433 fires)**, it proves the backtest is currently a "looser" strategy. It is firing roughly **4x more often** than your refined chart logic. If that high frequency is resulting in a high Sharpe, then your "Canonical" logic might actually be *too* restrictive, filtering out valid winners.

To move to a Single Source of Truth **without losing the magic**, follow this plan:

### 1. The "Snapshot" Protocol (Save the Ghost)
Before you change a single line of code, you must preserve the current "Fallback" logic state.
* **Instruction for Cursor:** "Create a new file `pipeline/src/orderflow_pipeline/strategies/legacy_fallback_logic.py` and copy the current logic from the `backtest_engine.py` fallback branch into it. Comment it as 'The High-Sharpe Ghost Logic'."
* **Why:** If the new unified logic underperforms, you have the exact mathematical "DNA" of the winner to revert to.

### 2. The "A/B" Comparison Test
Instead of replacing the old with the new, implement them side-by-side in the backtester.
* **The Goal:** Run the backtest twice. Once using the **Fires Table** (the 349 Bullseyes) and once using the **Internal Logic** (the 1433 Fallback signals).
* **The Metric:** If the 1433-fire version has a higher Sharpe, you don't kill it. Instead, you **update the Bullseye logic** to match the fallback logic.

### 3. Identify the "Missing" Alpha
There is a specific reason the fallback is firing 1,084 times more than the canonical. It’s likely one of three things:
1.  **Wick Precision:** The fallback might be less picky about the `wick_to_body` ratio.
2.  **Regime Overlap:** The fallback might be allowing trades in "Active-Light" or "Steady-Deep" cells that the Bullseye logic excludes.
3.  **Bias Sentiment:** The fallback might be ignoring the 1H/15M alignment that the Bullseyes require.

### 4. Implementation Path for Cursor
Give Cursor this specific instruction to ensure safety:

> "I need to unify my logic to a Single Source of Truth without losing the performance of the current 'fallback' engine.
>
> 1. **Data Export:** Export the current 'High Sharpe' fallback backtest trades to a CSV named `fallback_winners.csv`.
> 2. **Alignment Check:** Compare a sample of trades from `fallback_winners.csv` against the `fires` table. Identify 5 instances where the backtest entered but no Bullseye existed.
> 3. **The 'Merge'**: Update the Python Pipeline's `ValueEdgeReject` logic to incorporate whatever 'looser' parameters the fallback was using that contributed to the Sharpe.
> 4. **Visual Sync:** Once unified, ensure the chart Bullseyes now reflect the ~1400 signals (or whatever subset is valid) so I am finally trading what I see."



**Summary:** You aren't losing the Sharpe; you are **extracting the logic** that created it and making it the new "Canonical" standard. You're effectively saying: *"The backtest found a better version of my strategy than I drew on the chart; I'm going to update my chart to match the backtest."*