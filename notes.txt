"While we unify the signals, we need to fix the naive exit logic. The current 'flip-only' model is unrealistic.

Define Exit Params: Add support in the Python strategy module for stop_loss_ticks and take_profit_ticks.

Update Backtest Engine: Modify the broker logic to monitor these levels on every 1m bar instead of just waiting for the next signal.

Re-run Baseline: I want to see how the Sharpe holds up when every trade is forced to respect a hard stop loss."

Add SL/TP Logic: Update backtest_engine.py to support stop_loss_ticks and take_profit_ticks.

Intra-Bar Simulation: Since we use 1m bars, if a bar's High or Low hits our SL/TP, the trade must be closed at that price (with slippage).

Validation: Re-run the Value Edge test. If the Sharpe is still high with a 4-tick stop loss, then we have a real strategy. If it collapses, we need to know now."

