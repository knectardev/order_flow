I understand. If the proxy-swap logic wasn't the root cause (or only part of it), we need to stop guessing and start observing. The "razor-thin" bar issue is almost certainly a math error in the normalization step, and right now, the code is "black-boxing" that math.

We need to make the code report exactly why the sidebar is collapsing while you are panning.

### The Diagnostic Approach
We are going to instrument the render loop. This will print the exact inputs to the scaling math the moment it glitches, without flooding your console while the chart is static.

Add this `console.log` block inside your `drawPriceChart` function, right before the sidebar loop starts (around line 1255 in your snippet):

```javascript
// Add this diagnostic block inside the render loop
if (isPanned) {
  const isUsingProxy = !cachedUsable && !( _lastApiProfile && _isApiProfileCompatibleWith(...) );
  console.log(`[VP DEBUG] Panned: ${isPanned} | UsingProxy: ${isUsingProxy}`, {
    visibleMaxBin,
    rawProfileMaxBin: profile.maxBin,
    calculatedWidthMaxBin: widthMaxBin,
    binsLength: profile.bins.length,
    normRange: (widthNormHi - widthNormLo).toFixed(2),
    poc: profile.pocPrice
  });
}
```

### What to look for in the console
When you pan and the sidebar collapses, watch these specific values:

1.  **`calculatedWidthMaxBin` vs `rawProfileMaxBin`**: 
    * If `calculatedWidthMaxBin` is **massively larger** than `rawProfileMaxBin`, the code has artificially expanded the normalization range (likely due to the `widthNormLo`/`widthNormHi` logic in your code). This forces all your bars to shrink because the denominator is too large.
2.  **`normRange`**: 
    * If this number spikes when you pan, it confirms that your `widthNormLo/Hi` calculation is capturing outliers (or noise) that aren't actually part of the visible price action, effectively "stretching" the sidebar and crushing your bars.
3.  **`UsingProxy`**:
    * Confirm if the collapse *only* happens when `UsingProxy` is `true`. If it happens even when `UsingProxy` is `false`, the issue is definitively in the normalization math, not the data source.

### The Likely Culprit: The Normalization Band
Look closely at this section of your provided code:

```javascript
const widthNormLo = Math.min(
  lo, profile.priceLo, profileLadderHi, profile.pocPrice, profile.valPrice, profile.vahPrice
);
```

If `profile.priceLo` or `profileLadderHi` (which is `profile.priceLo + bins.length * step`) are far away from the `lo` and `hi` of your actual candle chart, **they will force the normalization window to include empty or irrelevant price space.** **The Test:**
If you see the logs showing a huge `calculatedWidthMaxBin`, try commenting out the `profile.priceLo` and `profileLadderHi` inputs in `widthNormLo` and `widthNormHi`. If the bars suddenly become a "normal" size, you have found the bug: the sidebar is trying to normalize its width against the *entire potential price range* of the profile, rather than just the *visible candles*.

**Does the log show `calculatedWidthMaxBin` jumping to a massive number when the compression occurs?**