# --- Raw Databento downloads (large, re-downloadable) ---
data/raw/
*.dbn.zst
*.zip

# --- Python ---
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Editor / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db

# --- Aggregated session JSON + index (large after full rebuilds; regenerate via pipeline). ---
data/bars/

# --- Local analytical DB artifact (large, machine-specific) ---
data/orderflow.duckdb
data/**/*.bak

# --- Pipeline rebuild logs (local only) ---
data/rebuild_log.txt
